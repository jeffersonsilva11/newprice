import React from 'react';

const Header = ({ title }) => {
  return (
    <header>
      <h1>{title}</h1>
      {/* TODO: Implementar header customizado se necessário */}
    </header>
  );
};

export default Header;
